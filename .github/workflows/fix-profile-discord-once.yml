name: One-time profile and Discord repair
on:
  push:
    branches: [fix-profile-discord]
permissions:
  contents: write
jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply surgical repair
        run: bash _maintenance/fix-profile-discord.sh
