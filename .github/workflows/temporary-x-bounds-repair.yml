name: Temporary X bounds branch trigger
on:
  push:
    branches: [fix-x-visual-bounds-final]
permissions:
  contents: read
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo "X bounds repair branch prepared"