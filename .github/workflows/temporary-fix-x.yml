name: One-time X visual balance repair
on:
  push:
    branches:
      - fix-x-visual-balance-final
permissions:
  contents: write
jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Final trigger marker
        run: echo "Final X balance repair requested"