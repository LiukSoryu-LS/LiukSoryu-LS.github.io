name: Temporary X final repair trigger
on:
  push:
    branches: [fix-x-final-bounds-20260907]
permissions:
  contents: read
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo "X final repair trigger - explicit bounds pass"