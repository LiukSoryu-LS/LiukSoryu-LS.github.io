name: Repair profile and Discord v2
on:
  push:
    branches: [fix-profile-discord]
permissions:
  contents: write
jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply repair
        run: bash _maintenance/fix-profile-discord.sh
